<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/club_logo"
        android:layout_width="24dp"
        android:layout_height="24dp"
        tools:src="@tools:sample/avatars"/>

    <TextView
        android:id="@+id/club_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        tools:text="Neftchi"/>

    <TextView android:id="@+id/season_year" tools:text="2025" style="@style/StatData" android:layout_width="60dp"/>
    <TextView android:id="@+id/games_stat" tools:text="17" style="@style/StatData.Small"/>
    <TextView android:id="@+id/goals_stat" tools:text="0" style="@style/StatData.Small"/>
</LinearLayout>